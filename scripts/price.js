// Get total price item
let price = document.getElementById('totalPrice');

// Initial value set of total price
let totalPrice = price.innerText = 0;

// Grand total
let grandTotal = document.getElementById('grandTotal');

// Initial value set of total price
let grandAmount = grandTotal.innerText = 0;
